# mosupisi-weather-service — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# HTTP client (async)
httpx==0.27.2

# Data validation
pydantic==2.9.2

# Database (async SQLite — swap connection string for PostgreSQL later)
aiosqlite==0.20.0

# Environment variables
python-dotenv==1.0.1

# --- Optional: activate when needed ---

# Twilio SMS (uncomment when TWILIO_ENABLED=true)
# twilio==9.3.5

# PostgreSQL driver (uncomment when migrating from SQLite)
# asyncpg==0.29.0
